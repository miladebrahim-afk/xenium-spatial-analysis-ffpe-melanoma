# Private provenance

This directory is reserved for private study metadata required for selected manuscript reproduction analyses.

The treatment-response annotation used for response-related analyses should be stored locally as:

`patient_treatment_response_annotation.xlsx`

This file contains study-specific metadata and must not be committed to the public repository.

The public repository includes only a metadata template under:

`data/metadata/patient_treatment_response_annotation.template.csv`
